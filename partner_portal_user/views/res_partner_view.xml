<odoo>
    <record id="add_partner_portal_field" model="ir.ui.view">
        <field name="name">res.partner.portal_field</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div class="o_address_format" position="after">
                <field name="is_portaluser" />
            </div>
        </field>
    </record>

    <record id="partner_portal_tree" model="ir.ui.view">
        <field name="name">res.partner.portal_tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="is_portaluser" optional="hide"/>
            </field>
        </field>
    </record>
</odoo>
