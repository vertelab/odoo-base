<odoo>
    <record id="calendar_event_view_form" model="ir.ui.view">
        <field name="name">calendar.event.view.form</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
        <field name="arch" type="xml">
            <field name="description" position="after">
                <field name="length" invisible="length == 0"/>
                <field name="width" invisible="width == 0"/>
                <field name="depth" invisible="depth == 0"/>
            </field>
        </field>
    </record>
</odoo>