<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Portal -->
    <template id="portal_my_home_menu_booking" name="Portal layout : booking menu entries" inherit_id="portal.portal_breadcrumbs">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'booking' or event" t-attf-class="breadcrumb-item #{'active ' if not event else ''}">
                <a t-if="event" t-attf-href="/my/bookings?{{ keep_query() }}">Bookings</a>
                <t t-else="">Bookings</t>
            </li>
            <li t-if="event" class="breadcrumb-item active">
                <span t-field="event.name"/>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_booking" name="Bookings" inherit_id="portal.portal_my_home">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_client_category_enable" t-value="True"/>
        </xpath>
        <div id="portal_client_category" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/base_booking/static/src/img/calendar.svg'"/>
                <t t-set="text">Follow, reschedule or cancel your bookings</t>
                <t t-set="title">Bookings</t>
                <t t-set="url" t-value="'/my/bookings'"/>
                <t t-set="placeholder_count" t-value="'booking_count'"/>
            </t>
        </div>
    </template>

    <template id="portal_my_bookings" name="My Bookings">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Bookings</t>
            </t>
            <t t-if="not bookings">
                <div class="alert alert-warning" role="alert">
                    <p class="mb-0">There is no booking linked to your account.</p>
                </div>
            </t>
            <t t-else="" t-call="portal.portal_table">
                <t t-set="bookings" t-value="grouped_bookings or bookings"/>
                <thead>
                    <tr>
                        <th>Booking Name</th>
                        <th class="text-end">Date</th>
                    </tr>
                </thead>
                <t t-foreach="bookings" t-as="appts">
                    <tbody t-if="grouped_bookings and not groupby == 'none'">
                        <t t-set="groupby_record" t-value="appts[0]"/>
                        <t t-set="appts" t-value="appts[1]"/>
                        <tr class="table-light">
                            <th>
                                <em class="fw-normal text-muted">Bookings by <t t-out="searchbar_groupby[groupby]['label']"/>:</em>
                                <span t-field="groupby_record.name"/>
                            </th>
                            <th class="text-end">
                                Total: <span class="text-muted" t-out="len(appts)"/>
                            </th>
                        </tr>
                    </tbody>
                    <tbody>
                        <t t-foreach="appts" t-as="booking">
                            <tr>
                                <td>
                                    <a t-attf-href="/calendar/view/#{booking.access_token}?partner_id=#{request.env.user.partner_id.id}">
                                        <t t-out="booking.booking_type_id.name"/>
                                        <t t-if="booking.booking_type_id.schedule_based_on == 'users'">
                                            with <t t-out="booking.user_id.name"/>
                                        </t>
                                    </a>
                                </td>
                                <td class="text-end"><span t-field="booking.start"/></td>
                            </tr>
                        </t>
                    </tbody>
                </t>
            </t>
        </t>
    </template>
</odoo>
