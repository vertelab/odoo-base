<?xml version="1.0"?>
<odoo>

    <record id="calendar_event_calendar_view_inherit_booking" model="ir.ui.view">
        <field name="name">calendar.event.view.calendar.inherit.booking</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_calendar"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='categ_ids']" position="after">
                <field name="booking_resource_ids" widget="many2many_tags" invisible="not booking_resource_ids" options="{'icon': 'fa fa-cubes'}"/>
            </xpath>
        </field>
    </record>
</odoo>
