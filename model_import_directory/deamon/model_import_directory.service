[Unit]
Description=Start model import directory
After=network.target

[Service]
User=odoo
ExecStart=/usr/local/bin/model_import_directory -d $(sudo su postgres -c "psql -At -c \"select datname from pg_database where datistemplate = false and 'postgres' <> datname;\" postgres|tr '\n' ','")

Restart=always

[Install]
WantedBy=multi-user.target
