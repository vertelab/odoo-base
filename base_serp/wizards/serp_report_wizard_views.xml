<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard Form View -->
    <record id="view_serp_report_wizard_form" model="ir.ui.view">
        <field name="name">serp.report.wizard.form</field>
        <field name="model">serp.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate SERP Report">
                <group>
                    <field name="partner_id" invisible="1"/>
                    <field name="date_from" string="Date" widget="daterange" options="{'end_date_field': 'date_to'}" />
                    <field name="date_to" invisible="1" />
                </group>
                <footer>
                    <button name="action_generate_report"
                            string="Generate Report"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_serp_report_wizard" model="ir.actions.act_window">
        <field name="name">Generate SERP Report</field>
        <field name="res_model">serp.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">form</field>
    </record>

</odoo>